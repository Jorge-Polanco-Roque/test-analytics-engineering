# ============================================================================
# BANK MARKETING DBT PROJECT - Environment Variables
# ============================================================================
# Este archivo documenta todas las variables de entorno requeridas
# Copia este archivo a .env y reemplaza los valores con tus credenciales
# ============================================================================

# ----------------------------------------------------------------------------
# GOOGLE CLOUD PLATFORM (GCP)
# ----------------------------------------------------------------------------

# ID del proyecto GCP para desarrollo
# Ejemplo: my-company-data-dev
DBT_GCP_PROJECT=your-gcp-project-id-here

# ID del proyecto GCP para staging (opcional)
# Ejemplo: my-company-data-staging
DBT_GCP_PROJECT_STAGING=your-gcp-project-staging-id-here

# ID del proyecto GCP para producción (opcional)
# Ejemplo: my-company-data-prod
DBT_GCP_PROJECT_PROD=your-gcp-project-prod-id-here

# Ruta al archivo JSON de credenciales de GCP Service Account
# Debe tener permisos: BigQuery Data Editor, BigQuery Job User
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/gcp-credentials.json

# Dataset de BigQuery para raw data
BIGQUERY_DATASET=bank_marketing_dev

# Región de BigQuery
BIGQUERY_LOCATION=US

# ----------------------------------------------------------------------------
# DBT CONFIGURATION
# ----------------------------------------------------------------------------

# Directorio de profiles de DBT (generalmente ~/.dbt)
DBT_PROFILES_DIR=~/.dbt

# Target de DBT (dev, staging, prod)
DBT_TARGET=dev

# Número de threads para DBT
DBT_THREADS=4

# ----------------------------------------------------------------------------
# PYTHON ENVIRONMENT
# ----------------------------------------------------------------------------

# Versión de Python (usada en Dockerfile)
PYTHON_VERSION=3.11

# Versión de DBT
DBT_VERSION=1.7.17

# ----------------------------------------------------------------------------
# OPTIONAL: NOTIFICATIONS
# ----------------------------------------------------------------------------

# Webhook URL para notificaciones de Slack (opcional)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Email para notificaciones de alertas (opcional)
# ALERT_EMAIL=alerts@yourcompany.com

# ----------------------------------------------------------------------------
# DOCKER CONFIGURATION (si usas Docker)
# ----------------------------------------------------------------------------

# Usuario y UID para Docker (evitar problemas de permisos)
# USER_ID=1000
# GROUP_ID=1000

# ----------------------------------------------------------------------------
# NOTAS DE USO
# ----------------------------------------------------------------------------
#
# 1. Para uso local:
#    - Copia este archivo: cp .env.example .env
#    - Llena los valores con tus credenciales reales
#    - Carga las variables: source .env (o usa python-dotenv)
#
# 2. Para GitHub Actions:
#    - Configura estos valores como GitHub Secrets
#    - Settings → Secrets and variables → Actions → New repository secret
#
# 3. Para Docker:
#    - Configura estas variables en docker-compose.yml
#    - O pasa variables con --env-file flag
#
# ============================================================================
